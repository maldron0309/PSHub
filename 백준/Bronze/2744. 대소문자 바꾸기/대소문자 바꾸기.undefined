#include <iostream>
#include <string>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string input;
	cin >> input;

	for (char& c : input) 
	{
		if (c >= 'a') c -= 32;
		else c += 32;
		cout << c;
	}

    return 0;
}
