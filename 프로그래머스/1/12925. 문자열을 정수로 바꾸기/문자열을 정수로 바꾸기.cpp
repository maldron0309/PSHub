#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int solution(string s) {
    int answer = 0;
    for (int i  = 1; i < s.size(); i++){
        answer = stoi(s);
    }
    return answer;
}